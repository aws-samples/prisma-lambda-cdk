{
  "name": "@aws-samples/backend",
  "private": true,
  "dependencies": {
    "@prisma/client": "^5.0.0"
  },
  "scripts": {
    "build": "tsc",
    "generate": "prisma generate",
    "migrate": "prisma migrate deploy"
  },
  "devDependencies": {
    "@types/aws-lambda": "^8.10.72",
    "@types/node": "^14.14.35",
    "prisma": "^5.0.0",
    "ts-node": "^9.1.1",
    "typescript": "^4.2.3",
    "esbuild": "^0.14.38"
  }
}
